const bienvenida = require('./bienvenida');
const gestionarAprendices = require('./gestionarAprendices'); // Asegúrate de que el archivo se llame igual
const consultarProgramas = require('./consultarProgramas');

module.exports = {
    bienvenida,
    gestionarAprendices,
    consultarProgramas
};