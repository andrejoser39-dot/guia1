const bienvenida = require('./bienvenida'); // Asegúrate de que el archivo se llame igual
const gestionarAprendices = require('./gestionarAprendices'); // Asegúrate de que el archivo se llame igual
const consultarProgramas = require('./consultarProgramas'); // Asegúrate de que el archivo se llame igual

module.exports = {
    bienvenida,
    gestionarAprendices,
    consultarProgramas
};